# Hybrid 和 H5 区别

`Hybrid` 优点:

- 体验更好, 跟 `Native` 体验基本一致

- 可快速迭代, 无需 `app` 审核

`Hybrid` 缺点:

- 开发成本高, 联调、测试、查 bug 都比较麻烦

- 运维成本高

适用的场景:

`Hybrid`: 产品的稳定功能, 体验要求高, 迭代频繁, 适合产品型

`H5`: 单次的运营活动(如 xx 红包)或不常用功能, 适合运营型